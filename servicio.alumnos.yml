#management:
 # endpoints:
    #web:
      #exposure:
        #include: hystrix.stream, info, health
        
# Discovery Server Access
#eureka:
  #client:
    #serviceUrl:
     # defaultZone: http://localhost:8761/eureka

server:
  port: 3334
  
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ms_alumnos?useSSL=false&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: root
  jpa:
    generate-ddl: true
